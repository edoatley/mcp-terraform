# API Test Configuration
# These properties can be overridden by environment variables or system properties
# to test against different environments (local, staging, production)

# REST API Base URL
# For local: http://localhost:8080
# For deployed: https://your-api-gateway-url.execute-api.region.amazonaws.com/stage
api.base.url=http://localhost:8080

# gRPC Configuration
grpc.host=localhost
grpc.port=9090

