# Test configuration - use random port for HTTP, fixed port for gRPC
server.port=0
grpc.server.port=9090

# Application Info
spring.application.name=todo-api-test

# Use in-memory storage for tests (no DynamoDB required)
spring.profiles.active=test,in-memory

